@inherits LayoutComponentBase
@inject NavigationManager NavManager

<div class="page">
    <div class="sidebar">
        <NavMenu @ref="_navMenu" />
    </div>
    
    <main>
        <MudAppBar Style="position: sticky; top: 0">
            <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit"
                           Edge="Edge.Start" OnClick="@(() => _navMenu?.Toggle())"/>
            <MudSpacer/>
            <img src="icon.svg" height="40px" alt="Logo" style="position: absolute; left: 50%; transform: translateX(-50%)"/>
            <MudSpacer/>
            <LoginDisplay/>
        </MudAppBar>
        <MudContainer Class="pa-0">
            @Body
        </MudContainer>
    </main>
</div>

@code {
    private NavMenu? _navMenu;

    protected override void OnInitialized()
    {
        NavManager.NavigateTo("/home");
    }
}
